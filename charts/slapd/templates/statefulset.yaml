apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "slapd.fullname" . }}
  labels:
    {{- include "slapd.labels" . | nindent 4 }}
spec:
  serviceName: {{ include "slapd.fullname" . }}
  replicas: 1
  selector:
    matchLabels:
      {{- include "slapd.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "slapd.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      initContainers:
        - name: init
          image: "{{ .Values.images.init.repository }}:{{ .Values.images.init.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.images.init.pullPolicy }}
          # bootstrap.sh writes /tmp/slapd.conf; "tmp" emptyDir below backs it
          # so we can keep the root FS read-only.
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          env:
            - name: LDAP_DOMAIN_DC
              value: {{ .Values.ldap.domain | quote }}
            {{- $secretName := (default (printf "%s-passwords" (include "slapd.fullname" .)) .Values.passwords.existingSecret) }}
            - name: LDAP_ADMIN_PW
              valueFrom:
                secretKeyRef:
                  name: {{ $secretName }}
                  key: admin-password
            - name: LDAP_ROOT_PW
              valueFrom:
                secretKeyRef:
                  name: {{ $secretName }}
                  key: root-password
            - name: LDAP_TLS_ENABLED
              value: {{ .Values.ldap.tls.enabled | quote }}
            - name: CONFIG_DIR
              value: "/ldap-config"
            - name: DATA_DIR
              value: "/ldap-data"
            {{- if .Values.ldap.tls.enabled }}
            - name: LDAP_TLS_CACERT_PATH
              value: "/etc/openldap/tls/ca.crt"
            - name: LDAP_TLS_CERT_PATH
              value: "/etc/openldap/tls/tls.crt"
            - name: LDAP_TLS_KEY_PATH
              value: "/etc/openldap/tls/tls.key"
            {{- end }}
          volumeMounts:
            - name: ldap-config
              mountPath: /ldap-config
            - name: ldap-data
              mountPath: /ldap-data
            - name: tmp
              mountPath: /tmp
            {{- if .Values.ldap.tls.enabled }}
            - name: ldap-tls
              mountPath: /etc/openldap/tls
              readOnly: true
            {{- end }}
      containers:
        - name: slapd
          image: "{{ .Values.images.slapd.repository }}:{{ .Values.images.slapd.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.images.slapd.pullPolicy }}
          args:
            - "-h"
            - "ldap://:1024/ ldaps://:1025/ ldapi://%2frun%2fopenldap%2fslapd.ldapi"
            - "-d"
            - {{ .Values.logLevel | toString | quote }}
            - "-F"
            - "/ldap-config/slapd.d"
          ports:
            - name: ldap
              containerPort: 1024
              protocol: TCP
            - name: ldaps
              containerPort: 1025
              protocol: TCP
          volumeMounts:
            - name: ldap-config
              mountPath: /ldap-config
            - name: ldap-data
              mountPath: /ldap-data
            - name: ldap-run
              mountPath: /run/openldap
            {{- if .Values.ldap.tls.enabled }}
            - name: ldap-tls
              mountPath: /etc/openldap/tls
              readOnly: true
            {{- end }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: ldap-config
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "slapd.fullname" . }}-config
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: ldap-data
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "slapd.fullname" . }}-data
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: ldap-run
          emptyDir: {}
        - name: tmp
          # Writable /tmp for bootstrap.sh ($TMP_CONF). Lets the init container
          # run with readOnlyRootFilesystem=true.
          emptyDir: {}
        {{- if .Values.ldap.tls.enabled }}
        - name: ldap-tls
          secret:
            secretName: {{ .Values.ldap.tls.secretName }}
        {{- end }}
