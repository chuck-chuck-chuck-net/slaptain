# Default values for slapd-test.

slapd:
  fullname: slapd
  domain: "dc=as8,dc=lab,dc=test"
  # Use the same secret generated by the slapd chart
  secretName: ""

  # Provide plain text passwords if not using an existing secret
  adminPassword: "admin"
  rootPassword: "admin"

image:
  repository: python
  tag: 3.14-trixie
  pullPolicy: IfNotPresent
