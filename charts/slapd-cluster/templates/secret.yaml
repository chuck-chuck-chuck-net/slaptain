{{- if not .Values.passwords.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "slapd-cluster.fullname" . }}-passwords
  labels:
    {{- include "slapd-cluster.labels" . | nindent 4 }}
type: Opaque
data:
  admin-password-hash: {{ required "passwords.adminPasswordHash is required when passwords.existingSecret is not set" .Values.passwords.adminPasswordHash | b64enc | quote }}
  root-password-hash: {{ required "passwords.rootPasswordHash is required when passwords.existingSecret is not set" .Values.passwords.rootPasswordHash | b64enc | quote }}
{{- end }}
